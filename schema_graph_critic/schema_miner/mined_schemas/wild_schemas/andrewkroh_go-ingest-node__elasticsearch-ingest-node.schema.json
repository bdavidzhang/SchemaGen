{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/andrewkroh/go-ingest-node/pipeline",
  "$ref": "#/$defs/Pipeline",
  "$defs": {
    "AppendProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to be appended to. Supports template snippets. Required."
        },
        "value": {
          "description": "The value to be appended. Supports template snippets. May specify only one of `value` or `copy_from`."
        },
        "media_type": {
          "type": "string",
          "description": "The media type for encoding `value`. Applies only when value is a template snippet. Must be one of `application/json`, `text/plain`, or `application/x-www-form-urlencoded`.",
          "default": "application/json"
        },
        "copy_from": {
          "type": "string",
          "description": "The origin field which will be appended to `field`, cannot set `value` simultaneously."
        },
        "allow_duplicates": {
          "type": "boolean",
          "description": "If `false`, the processor does not append values already present in the field.",
          "default": true
        },
        "ignore_empty_values": {
          "type": "boolean",
          "description": "If `true`, the processor will skip empty values from the source (e.g. empty strings, and null values), rather than appending them to the field.",
          "default": false
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Appends one or more values to an existing array if the field already exists and it is an array."
    },
    "AttachmentProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to get the base64 encoded field from. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and field does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "indexed_chars": {
          "type": "integer",
          "description": "The number of chars being used for extraction to prevent huge fields. Use `-1` for no limit.",
          "default": 100000
        },
        "indexed_chars_field": {
          "type": "string",
          "description": "Field name from which you can overwrite the number of chars being used for extraction."
        },
        "properties": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Array of properties to select to be stored. Can be `content`, `title`, `name`, `author`, `keywords`, `date`, `content_type`, `content_length`, `language`."
        },
        "target_field": {
          "type": "string",
          "description": "The field that will hold the attachment information.",
          "default": "attachment"
        },
        "remove_binary": {
          "type": "boolean",
          "description": "If true, the binary field will be removed from the document.",
          "default": false
        },
        "resource_name": {
          "type": "string",
          "description": "Field containing the name of the resource to decode. If specified, the processor passes this resource name to the underlying Tika library to enable Resource Name Based Detection."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "The attachment processor lets Elasticsearch extract file attachments in common formats (such as PPT, XLS, and PDF) by using the Apache text extraction library Tika."
    },
    "BytesProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to convert. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the converted value to. By default, the field is updated in-place.",
          "default": "field"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Converts a human readable byte value (for example `1kb`) to its value in bytes (for example `1024`)."
    },
    "CefProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field containing the CEF message. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the converted value to. By default, the `target_field` is 'cef'.",
          "default": "cef"
        },
        "ignore_empty_values": {
          "type": "boolean",
          "description": "If `true` and value is anempty string in extensions, the processor quietly exits without modifying the document.",
          "default": false
        },
        "timezone": {
          "type": "string",
          "description": "The timezone to use when parsing the date and when date math index supports resolves expressions into concrete index names.",
          "default": "UTC"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Converts a CEF message into a structured format."
    },
    "CircleProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "error_distance": {
          "type": "number",
          "description": "The difference between the resulting inscribed distance from center to side and the circle’s radius (measured in meters for `geo_shape`, unit-less for `shape`). Required."
        },
        "field": {
          "type": "string",
          "description": "The field to interpret as a circle. Either a string in WKT format or a map for GeoJSON. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "shape_type": {
          "type": "string",
          "enum": [
            "geo_shape",
            "shape"
          ],
          "description": "Which field mapping type is to be used when processing the circle: `geo_shape` or `shape`. Required."
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the polygon shape to By default, the field is updated in-place."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "error_distance",
        "field",
        "shape_type"
      ],
      "description": "Converts circle definitions of shapes to regular polygons which approximate them."
    },
    "CommunityIDProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "source_ip": {
          "type": "string",
          "description": "Field containing the source IP address.",
          "default": "source.ip"
        },
        "source_port": {
          "type": "string",
          "description": "Field containing the source port.",
          "default": "source.port"
        },
        "destination_ip": {
          "type": "string",
          "description": "Field containing the destination IP address.",
          "default": "destination.ip"
        },
        "destination_port": {
          "type": "string",
          "description": "Field containing the destination port.",
          "default": "destination.port"
        },
        "iana_number": {
          "type": "string",
          "description": "Field containing the IANA number.",
          "default": "network.iana_number"
        },
        "icmp_type": {
          "type": "string",
          "description": "Field containing the ICMP type.",
          "default": "icmp.type"
        },
        "icmp_code": {
          "type": "string",
          "description": "Field containing the ICMP code.",
          "default": "icmp.code"
        },
        "transport": {
          "type": "string",
          "description": "Field containing the transport protocol name or number. Used only when the iana_number field is not present. The following protocol names are currently supported: eigrp, gre, icmp, icmpv6, igmp, ipv6-icmp, ospf, pim, sctp, tcp, udp.",
          "default": "network.transport"
        },
        "target_field": {
          "type": "string",
          "description": "Output field for the community ID.",
          "default": "network.community_id"
        },
        "seed": {
          "type": "integer",
          "description": "Seed for the community ID hash. Must be between 0 and 65535 (inclusive). The seed can prevent hash collisions between network domains, such as a staging and production network that use the same addressing scheme.",
          "default": 0
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If true and any required fields are missing, the processor quietly exits without modifying the document.",
          "default": true
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "Computes the Community ID for network flow data as defined in the Community ID Specification."
    },
    "ConvertProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field whose value is to be converted. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the converted value to. By default, the `field` is updated in-place.",
          "default": "field"
        },
        "type": {
          "type": "string",
          "enum": [
            "integer",
            "long",
            "double",
            "float",
            "boolean",
            "ip",
            "string",
            "auto"
          ],
          "description": "The type to convert the existing value to. Required."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "type"
      ],
      "description": "Converts a field in the currently ingested document to a different type, such as converting a string to an integer."
    },
    "CsvProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "empty_value": {
          "description": "Value used to fill empty fields. Empty fields are skipped if this is not provided. An empty field is one with no value (2 consecutive separators) or empty quotes (`\"\"`)."
        },
        "field": {
          "type": "string",
          "description": "The field to extract data from. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document."
        },
        "quote": {
          "type": "string",
          "description": "Quote used in CSV, has to be single character string.",
          "default": "\""
        },
        "separator": {
          "type": "string",
          "description": "Separator used in CSV, has to be single character string.",
          "default": ","
        },
        "target_fields": {
          "description": "The array of fields to assign extracted values to. Required."
        },
        "trim": {
          "type": "boolean",
          "description": "Trim whitespaces in unquoted fields."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "target_fields"
      ],
      "description": "Extracts fields from CSV line out of a single text field within a document."
    },
    "DateIndexNameProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "date_formats": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "An array of the expected date formats for parsing dates / timestamps in the document being preprocessed. Can be a java time pattern or one of the following formats: ISO8601, UNIX, UNIX_MS, or TAI64N."
        },
        "date_rounding": {
          "type": "string",
          "description": "How to round the date when formatting the date into the index name. Valid values are: `y` (year), `M` (month), `w` (week), `d` (day), `h` (hour), `m` (minute) and `s` (second). Supports template snippets. Required."
        },
        "field": {
          "type": "string",
          "description": "The field to get the date or timestamp from. Required."
        },
        "index_name_format": {
          "type": "string",
          "description": "The format to be used when printing the parsed date into the index name. A valid java time pattern is expected here. Supports template snippets.",
          "default": "yyyy-MM-dd"
        },
        "index_name_prefix": {
          "type": "string",
          "description": "A prefix of the index name to be prepended before the printed date. Supports template snippets."
        },
        "locale": {
          "type": "string",
          "description": "The locale to use when parsing the date from the document being preprocessed, relevant when parsing month names or week days.",
          "default": "ENGLISH"
        },
        "timezone": {
          "type": "string",
          "description": "The timezone to use when parsing the date and when date math index supports resolves expressions into concrete index names.",
          "default": "UTC"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "date_rounding",
        "field"
      ],
      "description": "The purpose of this processor is to point documents to the right time based index based on a date or timestamp field in a document by using the date math index name support."
    },
    "DateProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to get the date from. Required."
        },
        "formats": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "An array of the expected date formats. Can be a java time pattern or one of the following formats: ISO8601, UNIX, UNIX_MS, or TAI64N. Required."
        },
        "locale": {
          "type": "string",
          "description": "The locale to use when parsing the date, relevant when parsing month names or week days. Supports template snippets.",
          "default": "ENGLISH"
        },
        "target_field": {
          "type": "string",
          "description": "The field that will hold the parsed date.",
          "default": "@timestamp"
        },
        "timezone": {
          "type": "string",
          "description": "The timezone to use when parsing the date. Supports template snippets.",
          "default": "UTC"
        },
        "output_format": {
          "type": "string",
          "description": "The format to use when writing the date to target_field. Must be a valid java time pattern.",
          "default": "yyyy-MM-dd'T'HH:mm:ss.SSSXXX"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "formats"
      ],
      "description": "Parses dates from fields, and then uses the date or timestamp as the timestamp for the document."
    },
    "DissectProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "append_separator": {
          "type": "string",
          "description": "The character(s) that separate the appended fields."
        },
        "field": {
          "type": "string",
          "description": "The field to dissect. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "pattern": {
          "type": "string",
          "description": "The pattern to apply to the field. Required."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "pattern"
      ],
      "description": "Extracts structured fields out of a single text field by matching the text field against a delimiter-based pattern."
    },
    "DotExpanderProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to expand into an object field. If set to `*`, all top-level fields will be expanded. Required."
        },
        "override": {
          "type": "boolean",
          "description": "Controls the behavior when there is already an existing nested object that conflicts with the expanded field. When `false`, the processor will merge conflicts by combining the old and the new values into an array. When `true`, the value from the expanded field will overwrite the existing value.",
          "default": false
        },
        "path": {
          "type": "string",
          "description": "The field that contains the field to expand. Only required if the field to expand is part another object field, because the `field` option can only understand leaf fields."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Expands a field with dots into an object field."
    },
    "DropProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "Drops the document without raising any errors."
    },
    "EnrichProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field in the input document that matches the policies match_field used to retrieve the enrichment data. Supports template snippets. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "max_matches": {
          "type": "integer",
          "description": "The maximum number of matched documents to include under the configured target field. The `target_field` will be turned into a json array if `max_matches` is higher than 1, otherwise `target_field` will become a json object. In order to avoid documents getting too large, the maximum allowed value is 128.",
          "default": 1
        },
        "override": {
          "type": "boolean",
          "description": "If processor will update fields with pre-existing non-null-valued field. When set to `false`, such fields will not be touched.",
          "default": true
        },
        "policy_name": {
          "type": "string",
          "description": "The name of the enrich policy to use. Required."
        },
        "shape_relation": {
          "type": "string",
          "enum": [
            "intersects",
            "disjoint",
            "within",
            "contains"
          ],
          "description": "A spatial relation operator used to match the geoshape of incoming documents to documents in the enrich index. This option is only used for `geo_match` enrich policy types.",
          "default": "INTERSECTS"
        },
        "target_field": {
          "type": "string",
          "description": "Field added to incoming documents to contain enrich data. This field contains both the `match_field` and `enrich_fields` specified in the enrich policy. Supports template snippets. Required."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "policy_name",
        "target_field"
      ],
      "description": "The `enrich` processor can enrich documents with data from another index."
    },
    "FailProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "message": {
          "type": "string",
          "description": "The error message thrown by the processor. Supports template snippets. Required."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "message"
      ],
      "description": "Raises an exception."
    },
    "FingerprintProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "fields": {
          "description": "Array of fields to include in the fingerprint. For objects, the processor hashes both the field key and value. For other fields, the processor hashes only the field value. Required."
        },
        "target_field": {
          "type": "string",
          "description": "Output field for the fingerprint.",
          "default": "fingerprint"
        },
        "salt": {
          "type": "string",
          "description": "Salt value for the hash function."
        },
        "method": {
          "type": "string",
          "enum": [
            "MD5",
            "SHA-1",
            "SHA-256",
            "SHA-512",
            "MurmurHash3"
          ],
          "description": "The hash method used to compute the fingerprint. Must be one of MD5, SHA-1, SHA-256, SHA-512, or MurmurHash3.",
          "default": "SHA-1"
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If true, the processor ignores any missing fields. If all fields are missing, the processor silently exits without modifying the document.",
          "default": false
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "fields"
      ],
      "description": "Computes a hash of the document’s content."
    },
    "ForeachProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "Field containing array or object values. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true`, the processor silently exits without changing the document if the `field` is `null` or missing.",
          "default": false
        },
        "processor": {
          "$ref": "#/$defs/ProcessorContainer",
          "description": "Ingest processor to run on each element. Required."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "processor"
      ],
      "description": "Runs an ingest processor on each element of an array or object."
    },
    "GeoGridProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to interpret as a geo-tile.= The field format is determined by the `tile_type`. Required."
        },
        "tile_type": {
          "type": "string",
          "enum": [
            "geotile",
            "geohex",
            "geohash"
          ],
          "description": "Three tile formats are understood: geohash, geotile and geohex. Required."
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the polygon shape to, by default, the `field` is updated in-place.",
          "default": "field"
        },
        "parent_field": {
          "type": "string",
          "description": "If specified and a parent tile exists, save that tile address to this field."
        },
        "children_field": {
          "type": "string",
          "description": "If specified and children tiles exist, save those tile addresses to this field as an array of strings."
        },
        "non_children_field": {
          "type": "string",
          "description": "If specified and intersecting non-child tiles exist, save their addresses to this field as an array of strings."
        },
        "precision_field": {
          "type": "string",
          "description": "If specified, save the tile precision (zoom) as an integer to this field."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "target_format": {
          "type": "string",
          "enum": [
            "geojson",
            "wkt"
          ],
          "description": "Which format to save the generated polygon in.",
          "default": "geojson"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "tile_type"
      ],
      "description": "Converts geo-grid definitions of grid tiles or cells to regular bounding boxes or polygons which describe their shape."
    },
    "GeoIpProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "database_file": {
          "type": "string",
          "description": "The database filename referring to a database the module ships with (GeoLite2-City.mmdb, GeoLite2-Country.mmdb, or GeoLite2-ASN.mmdb) or a custom database in the ingest-geoip config directory.",
          "default": "GeoLite2-City.mmdb"
        },
        "field": {
          "type": "string",
          "description": "The field to get the ip address from for the geographical lookup. Required."
        },
        "first_only": {
          "type": "boolean",
          "description": "If `true`, only the first found geoip data will be returned, even if the field contains an array.",
          "default": true
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "properties": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Controls what properties are added to the `target_field` based on the geoip lookup."
        },
        "target_field": {
          "type": "string",
          "description": "The field that will hold the geographical information looked up from the MaxMind database.",
          "default": "geoip"
        },
        "download_database_on_pipeline_creation": {
          "type": "boolean",
          "description": "If `true` (and if `ingest.geoip.downloader.eager.download` is `false`), the missing database is downloaded when the pipeline is created. Else, the download is triggered by when the pipeline is used as the `default_pipeline` or `final_pipeline` in an index."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "The `geoip` processor adds information about the geographical location of an IPv4 or IPv6 address."
    },
    "GrokProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "ecs_compatibility": {
          "type": "string",
          "description": "Must be disabled or v1. If v1, the processor uses patterns with Elastic Common Schema (ECS) field names.",
          "default": "disabled"
        },
        "field": {
          "type": "string",
          "description": "The field to use for grok expression parsing. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "pattern_definitions": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "A map of pattern-name and pattern tuples defining custom patterns to be used by the current processor. Patterns matching existing names will override the pre-existing definition."
        },
        "patterns": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "An ordered list of grok expression to match and extract named captures with. Returns on the first expression in the list that matches. Required."
        },
        "trace_match": {
          "type": "boolean",
          "description": "When `true`, `_ingest._grok_match_index` will be inserted into your matched document’s metadata with the index into the pattern found in `patterns` that matched.",
          "default": false
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "patterns"
      ],
      "description": "Extracts structured fields out of a single text field within a document."
    },
    "GsubProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to apply the replacement to. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "pattern": {
          "type": "string",
          "description": "The pattern to be replaced. Required."
        },
        "replacement": {
          "type": "string",
          "description": "The string to replace the matching patterns with. Required."
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the converted value to By default, the `field` is updated in-place.",
          "default": "field"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "pattern",
        "replacement"
      ],
      "description": "Converts a string field by applying a regular expression and a replacement."
    },
    "HtmlStripProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The string-valued field to remove HTML tags from. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document,.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the converted value to By default, the `field` is updated in-place.",
          "default": "field"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Removes HTML tags from the field."
    },
    "InferenceConfig": {
      "properties": {
        "regression": {
          "$ref": "#/$defs/InferenceConfigRegression",
          "description": "Regression configuration for inference."
        },
        "classification": {
          "$ref": "#/$defs/InferenceConfigClassification",
          "description": "Classification configuration for inference."
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "InferenceConfigClassification": {
      "properties": {
        "num_top_classes": {
          "type": "integer",
          "description": "Specifies the number of top class predictions to return.",
          "default": 0
        },
        "num_top_feature_importance_values": {
          "type": "integer",
          "description": "Specifies the maximum number of feature importance values per document.",
          "default": 0
        },
        "results_field": {
          "type": "string",
          "description": "The field that is added to incoming documents to contain the inference prediction.",
          "default": "\u003cdependent_variable\u003e_prediction"
        },
        "top_classes_results_field": {
          "type": "string",
          "description": "Specifies the field to which the top classes are written.",
          "default": "top_classes"
        },
        "prediction_field_type": {
          "type": "string",
          "description": "Specifies the type of the predicted field to write. Valid values are: `string`, `number`, `boolean`."
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "InferenceConfigRegression": {
      "properties": {
        "results_field": {
          "type": "string",
          "description": "The field that is added to incoming documents to contain the inference prediction.",
          "default": "\u003cdependent_variable\u003e_prediction"
        },
        "num_top_feature_importance_values": {
          "type": "integer",
          "description": "Specifies the maximum number of feature importance values per document.",
          "default": 0
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "InferenceProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "model_id": {
          "type": "string",
          "description": "The ID or alias for the trained model, or the ID of the deployment. Required."
        },
        "target_field": {
          "type": "string",
          "description": "Field added to incoming documents to contain results objects.",
          "default": "ml.inference.\u003cprocessor_tag\u003e"
        },
        "field_map": {
          "type": "object",
          "description": "Maps the document field names to the known field names of the model. This mapping takes precedence over any default mappings provided in the model configuration."
        },
        "inference_config": {
          "$ref": "#/$defs/InferenceConfig",
          "description": "Contains the inference type and its options."
        },
        "input_output": {
          "description": "Input fields for inference and output (destination) fields for the inference results. This option is incompatible with the target_field and field_map options."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If true and any of the input fields defined in input_ouput are missing then those missing fields are quietly ignored, otherwise a missing field causes a failure. Only applies when using input_output configurations to explicitly list the input fields."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "model_id"
      ],
      "description": "Uses a pre-trained data frame analytics model or a model deployed for natural language processing tasks to infer against the data that is being ingested in the pipeline."
    },
    "IpLocationProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "database_file": {
          "type": "string",
          "description": "The database filename referring to a database the module ships with (GeoLite2-City.mmdb, GeoLite2-Country.mmdb, or GeoLite2-ASN.mmdb) or a custom database in the ingest-geoip config directory.",
          "default": "GeoLite2-City.mmdb"
        },
        "field": {
          "type": "string",
          "description": "The field to get the ip address from for the geographical lookup. Required."
        },
        "first_only": {
          "type": "boolean",
          "description": "If `true`, only the first found IP location data will be returned, even if the field contains an array.",
          "default": true
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "properties": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Controls what properties are added to the `target_field` based on the IP location lookup."
        },
        "target_field": {
          "type": "string",
          "description": "The field that will hold the geographical information looked up from the MaxMind database.",
          "default": "geoip"
        },
        "download_database_on_pipeline_creation": {
          "type": "boolean",
          "description": "If `true` (and if `ingest.geoip.downloader.eager.download` is `false`), the missing database is downloaded when the pipeline is created. Else, the download is triggered by when the pipeline is used as the `default_pipeline` or `final_pipeline` in an index."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Currently an undocumented alias for GeoIP Processor."
    },
    "JoinProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "Field containing array values to join. Required."
        },
        "separator": {
          "type": "string",
          "description": "The separator character. Required."
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the joined value to. By default, the field is updated in-place.",
          "default": "field"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "separator"
      ],
      "description": "Joins each element of an array into a single string using a separator character between each element."
    },
    "JsonProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "add_to_root": {
          "type": "boolean",
          "description": "Flag that forces the parsed JSON to be added at the top level of the document. `target_field` must not be set when this option is chosen.",
          "default": false
        },
        "add_to_root_conflict_strategy": {
          "type": "string",
          "enum": [
            "replace",
            "merge"
          ],
          "description": "When set to `replace`, root fields that conflict with fields from the parsed JSON will be overridden. When set to `merge`, conflicting fields will be merged. Only applicable `if add_to_root` is set to true."
        },
        "allow_duplicate_keys": {
          "type": "boolean",
          "description": "When set to `true`, the JSON parser will not fail if the JSON contains duplicate keys. Instead, the last encountered value for any duplicate key wins.",
          "default": false
        },
        "field": {
          "type": "string",
          "description": "The field to be parsed. Required."
        },
        "target_field": {
          "type": "string",
          "description": "The field that the converted structured object will be written into. Any existing content in this field will be overwritten.",
          "default": "field"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Converts a JSON string into a structured JSON object."
    },
    "KeyValueProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "exclude_keys": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "List of keys to exclude from document."
        },
        "field": {
          "type": "string",
          "description": "The field to be parsed. Supports template snippets. Required."
        },
        "field_split": {
          "type": "string",
          "description": "Regex pattern to use for splitting key-value pairs. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "include_keys": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "List of keys to filter and insert into document. Defaults to including all keys."
        },
        "prefix": {
          "type": "string",
          "description": "Prefix to be added to extracted keys."
        },
        "strip_brackets": {
          "type": "boolean",
          "description": "If `true`. strip brackets `()`, `\u003c\u003e`, `[]` as well as quotes `'` and `\"` from extracted values.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "The field to insert the extracted keys into. Defaults to the root of the document. Supports template snippets."
        },
        "trim_key": {
          "type": "string",
          "description": "String of characters to trim from extracted keys."
        },
        "trim_value": {
          "type": "string",
          "description": "String of characters to trim from extracted values."
        },
        "value_split": {
          "type": "string",
          "description": "Regex pattern to use for splitting the key from the value within a key-value pair. Required."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "field_split",
        "value_split"
      ],
      "description": "This processor helps automatically parse messages (or specific event fields) which are of the `foo=bar` variety."
    },
    "LowercaseProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to make lowercase. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the converted value to. By default, the field is updated in-place.",
          "default": "field"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Converts a string to its lowercase equivalent."
    },
    "Metadata": {
      "type": "object"
    },
    "NetworkDirectionProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "source_ip": {
          "type": "string",
          "description": "Field containing the source IP address.",
          "default": "source.ip"
        },
        "destination_ip": {
          "type": "string",
          "description": "Field containing the destination IP address.",
          "default": "destination.ip"
        },
        "target_field": {
          "type": "string",
          "description": "Output field for the network direction.",
          "default": "network.direction"
        },
        "internal_networks": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "List of internal networks. Supports IPv4 and IPv6 addresses and ranges in CIDR notation. Also supports the named ranges listed below. These may be constructed with template snippets. Must specify only one of internal_networks or internal_networks_field."
        },
        "internal_networks_field": {
          "type": "string",
          "description": "A field on the given document to read the internal_networks configuration from."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If true and any required fields are missing, the processor quietly exits without modifying the document.",
          "default": true
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "Calculates the network direction given a source IP address, destination IP address, and a list of internal networks."
    },
    "Pipeline": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the ingest pipeline."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Processors to run immediately after a processor failure."
        },
        "processors": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Processors used to perform transformations on documents before indexing. Processors run sequentially in the order specified."
        },
        "version": {
          "type": "integer",
          "description": "Version number used by external systems to track ingest pipelines."
        },
        "deprecated": {
          "type": "boolean",
          "description": "Marks this ingest pipeline as deprecated. When a deprecated ingest pipeline is referenced as the default or final pipeline when creating or updating a non-deprecated index template, Elasticsearch will emit a deprecation warning.",
          "default": false
        },
        "_meta": {
          "$ref": "#/$defs/Metadata",
          "description": "Arbitrary metadata about the ingest pipeline. This map is not automatically generated by Elasticsearch."
        },
        "created_date": {
          "description": "Date and time when the pipeline was created. Only returned if the `human` query parameter is `true`."
        },
        "created_date_millis": {
          "type": "integer",
          "description": "Date and time when the pipeline was created, in milliseconds since the epoch."
        },
        "modified_date": {
          "description": "Date and time when the pipeline was last modified. Only returned if the `human` query parameter is `true`."
        },
        "modified_date_millis": {
          "type": "integer",
          "description": "Date and time when the pipeline was last modified, in milliseconds since the epoch."
        },
        "field_access_pattern": {
          "type": "string",
          "enum": [
            "classic",
            "flexible"
          ],
          "description": "Controls how processors in this pipeline should read and write data on a document's source.",
          "default": "classic"
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "PipelineProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "name": {
          "type": "string",
          "description": "The name of the pipeline to execute. Supports template snippets. Required."
        },
        "ignore_missing_pipeline": {
          "type": "boolean",
          "description": "Whether to ignore missing pipelines instead of failing.",
          "default": false
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name"
      ],
      "description": "Executes another pipeline."
    },
    "ProcessorContainer": {
      "properties": {
        "append": {
          "$ref": "#/$defs/AppendProcessor",
          "description": "Appends one or more values to an existing array if the field already exists and it is an array. Converts a scalar to an array and appends one or more values to it if the field exists and it is a scalar. Creates an array containing the provided values if the field doesn’t exist. Accepts a single value or an array of values."
        },
        "attachment": {
          "$ref": "#/$defs/AttachmentProcessor",
          "description": "The attachment processor lets Elasticsearch extract file attachments in common formats (such as PPT, XLS, and PDF) by using the Apache text extraction library Tika."
        },
        "bytes": {
          "$ref": "#/$defs/BytesProcessor",
          "description": "Converts a human readable byte value (for example `1kb`) to its value in bytes (for example `1024`). If the field is an array of strings, all members of the array will be converted. Supported human readable units are \"b\", \"kb\", \"mb\", \"gb\", \"tb\", \"pb\" case insensitive. An error will occur if the field is not a supported format or resultant value exceeds 2^63."
        },
        "cef": {
          "$ref": "#/$defs/CefProcessor",
          "description": "Converts a CEF message into a structured format."
        },
        "circle": {
          "$ref": "#/$defs/CircleProcessor",
          "description": "Converts circle definitions of shapes to regular polygons which approximate them."
        },
        "community_id": {
          "$ref": "#/$defs/CommunityIDProcessor",
          "description": "Computes the Community ID for network flow data as defined in the Community ID Specification. You can use a community ID to correlate network events related to a single flow."
        },
        "convert": {
          "$ref": "#/$defs/ConvertProcessor",
          "description": "Converts a field in the currently ingested document to a different type, such as converting a string to an integer. If the field value is an array, all members will be converted."
        },
        "csv": {
          "$ref": "#/$defs/CsvProcessor",
          "description": "Extracts fields from CSV line out of a single text field within a document. Any empty field in CSV will be skipped."
        },
        "date": {
          "$ref": "#/$defs/DateProcessor",
          "description": "Parses dates from fields, and then uses the date or timestamp as the timestamp for the document."
        },
        "date_index_name": {
          "$ref": "#/$defs/DateIndexNameProcessor",
          "description": "The purpose of this processor is to point documents to the right time based index based on a date or timestamp field in a document by using the date math index name support."
        },
        "dissect": {
          "$ref": "#/$defs/DissectProcessor",
          "description": "Extracts structured fields out of a single text field by matching the text field against a delimiter-based pattern."
        },
        "dot_expander": {
          "$ref": "#/$defs/DotExpanderProcessor",
          "description": "Expands a field with dots into an object field. This processor allows fields with dots in the name to be accessible by other processors in the pipeline. Otherwise these fields can’t be accessed by any processor."
        },
        "drop": {
          "$ref": "#/$defs/DropProcessor",
          "description": "Drops the document without raising any errors. This is useful to prevent the document from getting indexed based on some condition."
        },
        "enrich": {
          "$ref": "#/$defs/EnrichProcessor",
          "description": "The `enrich` processor can enrich documents with data from another index."
        },
        "fail": {
          "$ref": "#/$defs/FailProcessor",
          "description": "Raises an exception. This is useful for when you expect a pipeline to fail and want to relay a specific message to the requester."
        },
        "fingerprint": {
          "$ref": "#/$defs/FingerprintProcessor",
          "description": "Computes a hash of the document’s content. You can use this hash for content fingerprinting."
        },
        "foreach": {
          "$ref": "#/$defs/ForeachProcessor",
          "description": "Runs an ingest processor on each element of an array or object."
        },
        "ip_location": {
          "$ref": "#/$defs/IpLocationProcessor",
          "description": "Currently an undocumented alias for GeoIP Processor."
        },
        "geo_grid": {
          "$ref": "#/$defs/GeoGridProcessor",
          "description": "Converts geo-grid definitions of grid tiles or cells to regular bounding boxes or polygons which describe their shape. This is useful if there is a need to interact with the tile shapes as spatially indexable fields."
        },
        "geoip": {
          "$ref": "#/$defs/GeoIpProcessor",
          "description": "The `geoip` processor adds information about the geographical location of an IPv4 or IPv6 address."
        },
        "grok": {
          "$ref": "#/$defs/GrokProcessor",
          "description": "Extracts structured fields out of a single text field within a document. You choose which field to extract matched fields from, as well as the grok pattern you expect will match. A grok pattern is like a regular expression that supports aliased expressions that can be reused."
        },
        "gsub": {
          "$ref": "#/$defs/GsubProcessor",
          "description": "Converts a string field by applying a regular expression and a replacement. If the field is an array of string, all members of the array will be converted. If any non-string values are encountered, the processor will throw an exception."
        },
        "html_strip": {
          "$ref": "#/$defs/HtmlStripProcessor",
          "description": "Removes HTML tags from the field. If the field is an array of strings, HTML tags will be removed from all members of the array."
        },
        "inference": {
          "$ref": "#/$defs/InferenceProcessor",
          "description": "Uses a pre-trained data frame analytics model or a model deployed for natural language processing tasks to infer against the data that is being ingested in the pipeline."
        },
        "join": {
          "$ref": "#/$defs/JoinProcessor",
          "description": "Joins each element of an array into a single string using a separator character between each element. Throws an error when the field is not an array."
        },
        "json": {
          "$ref": "#/$defs/JsonProcessor",
          "description": "Converts a JSON string into a structured JSON object."
        },
        "kv": {
          "$ref": "#/$defs/KeyValueProcessor",
          "description": "This processor helps automatically parse messages (or specific event fields) which are of the `foo=bar` variety."
        },
        "lowercase": {
          "$ref": "#/$defs/LowercaseProcessor",
          "description": "Converts a string to its lowercase equivalent. If the field is an array of strings, all members of the array will be converted."
        },
        "network_direction": {
          "$ref": "#/$defs/NetworkDirectionProcessor",
          "description": "Calculates the network direction given a source IP address, destination IP address, and a list of internal networks."
        },
        "pipeline": {
          "$ref": "#/$defs/PipelineProcessor",
          "description": "Executes another pipeline."
        },
        "redact": {
          "$ref": "#/$defs/RedactProcessor",
          "description": "The Redact processor uses the Grok rules engine to obscure text in the input document matching the given Grok patterns. The processor can be used to obscure Personal Identifying Information (PII) by configuring it to detect known patterns such as email or IP addresses. Text that matches a Grok pattern is replaced with a configurable string such as `\u003cEMAIL\u003e` where an email address is matched or simply replace all matches with the text `\u003cREDACTED\u003e` if preferred."
        },
        "registered_domain": {
          "$ref": "#/$defs/RegisteredDomainProcessor",
          "description": "Extracts the registered domain (also known as the effective top-level domain or eTLD), sub-domain, and top-level domain from a fully qualified domain name (FQDN). Uses the registered domains defined in the Mozilla Public Suffix List."
        },
        "remove": {
          "$ref": "#/$defs/RemoveProcessor",
          "description": "Removes existing fields. If one field doesn’t exist, an exception will be thrown."
        },
        "rename": {
          "$ref": "#/$defs/RenameProcessor",
          "description": "Renames an existing field. If the field doesn’t exist or the new name is already used, an exception will be thrown."
        },
        "reroute": {
          "$ref": "#/$defs/RerouteProcessor",
          "description": "Routes a document to another target index or data stream. When setting the `destination` option, the target is explicitly specified and the dataset and namespace options can’t be set. When the `destination` option is not set, this processor is in a data stream mode. Note that in this mode, the reroute processor can only be used on data streams that follow the data stream naming scheme."
        },
        "script": {
          "$ref": "#/$defs/ScriptProcessor",
          "description": "Runs an inline or stored script on incoming documents. The script runs in the `ingest` context."
        },
        "set": {
          "$ref": "#/$defs/SetProcessor",
          "description": "Adds a field with the specified value. If the field already exists, its value will be replaced with the provided one."
        },
        "set_security_user": {
          "$ref": "#/$defs/SetSecurityUserProcessor",
          "description": "Sets user-related details (such as `username`, `roles`, `email`, `full_name`, `metadata`, `api_key`, `realm` and `authentication_type`) from the current authenticated user to the current document by pre-processing the ingest."
        },
        "sort": {
          "$ref": "#/$defs/SortProcessor",
          "description": "Sorts the elements of an array ascending or descending. Homogeneous arrays of numbers will be sorted numerically, while arrays of strings or heterogeneous arrays of strings + numbers will be sorted lexicographically. Throws an error when the field is not an array."
        },
        "split": {
          "$ref": "#/$defs/SplitProcessor",
          "description": "Splits a field into an array using a separator character. Only works on string fields."
        },
        "terminate": {
          "$ref": "#/$defs/TerminateProcessor",
          "description": "Terminates the current ingest pipeline, causing no further processors to be run. This will normally be executed conditionally, using the `if` option."
        },
        "trim": {
          "$ref": "#/$defs/TrimProcessor",
          "description": "Trims whitespace from a field. If the field is an array of strings, all members of the array will be trimmed. This only works on leading and trailing whitespace."
        },
        "uppercase": {
          "$ref": "#/$defs/UppercaseProcessor",
          "description": "Converts a string to its uppercase equivalent. If the field is an array of strings, all members of the array will be converted."
        },
        "urldecode": {
          "$ref": "#/$defs/UrlDecodeProcessor",
          "description": "URL-decodes a string. If the field is an array of strings, all members of the array will be decoded."
        },
        "uri_parts": {
          "$ref": "#/$defs/UriPartsProcessor",
          "description": "Parses a Uniform Resource Identifier (URI) string and extracts its components as an object. This URI object includes properties for the URI’s domain, path, fragment, port, query, scheme, user info, username, and password."
        },
        "user_agent": {
          "$ref": "#/$defs/UserAgentProcessor",
          "description": "The `user_agent` processor extracts details from the user agent string a browser sends with its web requests. This processor adds this information by default under the `user_agent` field."
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "RedactProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to be redacted. Required."
        },
        "patterns": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "A list of grok expressions to match and redact named captures with. Required."
        },
        "pattern_definitions": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "prefix": {
          "type": "string",
          "description": "Start a redacted section with this token.",
          "default": "\u003c"
        },
        "suffix": {
          "type": "string",
          "description": "End a redacted section with this token.",
          "default": "\u003e"
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "skip_if_unlicensed": {
          "type": "boolean",
          "description": "If `true` and the current license does not support running redact processors, then the processor quietly exits without modifying the document.",
          "default": false
        },
        "trace_redact": {
          "type": "boolean",
          "description": "If `true` then ingest metadata `_ingest._redact._is_redacted` is set to `true` if the document has been redacted.",
          "default": false
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "patterns"
      ],
      "description": "The Redact processor uses the Grok rules engine to obscure text in the input document matching the given Grok patterns."
    },
    "RegisteredDomainProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "Field containing the source FQDN. Required."
        },
        "target_field": {
          "type": "string",
          "description": "Object field containing extracted domain components. If an empty string, the processor adds components to the document’s root."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If true and any required fields are missing, the processor quietly exits without modifying the document.",
          "default": true
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Extracts the registered domain (also known as the effective top-level domain or eTLD), sub-domain, and top-level domain from a fully qualified domain name (FQDN)."
    },
    "RemoveProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "description": "Fields to be removed. Supports template snippets. Required."
        },
        "keep": {
          "description": "Fields to be kept. When set, all fields other than those specified are removed."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Removes existing fields."
    },
    "RenameProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to be renamed. Supports template snippets. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "The new name of the field. Supports template snippets. Required."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "target_field"
      ],
      "description": "Renames an existing field."
    },
    "RerouteProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "destination": {
          "type": "string",
          "description": "A static value for the target. Can’t be set when the dataset or namespace option is set."
        },
        "dataset": {
          "description": "Field references or a static value for the dataset part of the data stream name. In addition to the criteria for index names, cannot contain - and must be no longer than 100 characters. Example values are nginx.access and nginx.error.  Supports field references with a mustache-like syntax (denoted as {{double}} or {{{triple}}} curly braces). When resolving field references, the processor replaces invalid characters with _. Uses the \u003cdataset\u003e part of the index name as a fallback if all field references resolve to a null, missing, or non-string value.  default {{data_stream.dataset}}."
        },
        "namespace": {
          "description": "Field references or a static value for the namespace part of the data stream name. See the criteria for index names for allowed characters. Must be no longer than 100 characters.  Supports field references with a mustache-like syntax (denoted as {{double}} or {{{triple}}} curly braces). When resolving field references, the processor replaces invalid characters with _. Uses the \u003cnamespace\u003e part of the index name as a fallback if all field references resolve to a null, missing, or non-string value.  default {{data_stream.namespace}}."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "Routes a document to another target index or data stream."
    },
    "ScriptProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "id": {
          "type": "string",
          "description": "ID of a stored script. If no `source` is specified, this parameter is required."
        },
        "lang": {
          "type": "string",
          "enum": [
            "painless",
            "expression",
            "mustache",
            "java"
          ],
          "description": "Script language.",
          "default": "painless"
        },
        "params": {
          "type": "object",
          "description": "Object containing parameters for the script."
        },
        "source": {
          "type": "string",
          "description": "Inline script. If no `id` is specified, this parameter is required."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "Runs an inline or stored script on incoming documents."
    },
    "SetProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "copy_from": {
          "type": "string",
          "description": "The origin field which will be copied to `field`, cannot set `value` simultaneously. Supported data types are `boolean`, `number`, `array`, `object`, `string`, `date`, etc."
        },
        "field": {
          "type": "string",
          "description": "The field to insert, upsert, or update. Supports template snippets. Required."
        },
        "ignore_empty_value": {
          "type": "boolean",
          "description": "If `true` and `value` is a template snippet that evaluates to `null` or the empty string, the processor quietly exits without modifying the document.",
          "default": false
        },
        "media_type": {
          "type": "string",
          "description": "The media type for encoding `value`. Applies only when value is a template snippet. Must be one of `application/json`, `text/plain`, or `application/x-www-form-urlencoded`.",
          "default": "application/json"
        },
        "override": {
          "type": "boolean",
          "description": "If `true` processor will update fields with pre-existing non-null-valued field. When set to `false`, such fields will not be touched.",
          "default": true
        },
        "value": {
          "description": "The value to be set for the field. Supports template snippets. May specify only one of `value` or `copy_from`."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Adds a field with the specified value."
    },
    "SetSecurityUserProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to store the user information into. Required."
        },
        "properties": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Controls what user related properties are added to the field."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Sets user-related details (such as `username`, `roles`, `email`, `full_name`, `metadata`, `api_key`, `realm` and `authentication_type`) from the current authenticated user to the current document by pre-processing the ingest."
    },
    "SortProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to be sorted. Required."
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "The sort order to use. Accepts `\"asc\"` or `\"desc\"`.",
          "default": "asc"
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the sorted value to. By default, the field is updated in-place."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Sorts the elements of an array ascending or descending."
    },
    "SplitProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to split. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "preserve_trailing": {
          "type": "boolean",
          "description": "Preserves empty trailing fields, if any.",
          "default": false
        },
        "separator": {
          "type": "string",
          "description": "A regex which matches the separator, for example, `,` or `\\s+`. Required."
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the split value to. By default, the field is updated in-place.",
          "default": "field"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field",
        "separator"
      ],
      "description": "Splits a field into an array using a separator character."
    },
    "TerminateProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "Terminates the current ingest pipeline, causing no further processors to be run."
    },
    "TrimProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The string-valued field to trim whitespace from. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the trimmed value to. By default, the field is updated in-place.",
          "default": "field"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Trims whitespace from a field."
    },
    "UppercaseProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to make uppercase. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the converted value to. By default, the field is updated in-place.",
          "default": "field"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Converts a string to its uppercase equivalent."
    },
    "UriPartsProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "Field containing the URI string. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "keep_original": {
          "type": "boolean",
          "description": "If `true`, the processor copies the unparsed URI to `\u003ctarget_field\u003e.original`.",
          "default": true
        },
        "remove_if_successful": {
          "type": "boolean",
          "description": "If `true`, the processor removes the `field` after parsing the URI string. If parsing fails, the processor does not remove the `field`.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "Output field for the URI object.",
          "default": "url"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "Parses a Uniform Resource Identifier (URI) string and extracts its components as an object."
    },
    "UrlDecodeProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field to decode. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist or is `null`, the processor quietly exits without modifying the document.",
          "default": false
        },
        "target_field": {
          "type": "string",
          "description": "The field to assign the converted value to. By default, the field is updated in-place.",
          "default": "field"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "URL-decodes a string."
    },
    "UserAgentProcessor": {
      "properties": {
        "description": {
          "type": "string",
          "description": "Description of the processor. Useful for describing the purpose of the processor or its configuration."
        },
        "if": {
          "type": "string",
          "description": "Conditionally execute the processor."
        },
        "ignore_failure": {
          "type": "boolean",
          "description": "Ignore failures for the processor."
        },
        "on_failure": {
          "items": {
            "$ref": "#/$defs/ProcessorContainer"
          },
          "type": "array",
          "description": "Handle failures for the processor."
        },
        "tag": {
          "type": "string",
          "description": "Identifier for the processor. Useful for debugging and metrics."
        },
        "field": {
          "type": "string",
          "description": "The field containing the user agent string. Required."
        },
        "ignore_missing": {
          "type": "boolean",
          "description": "If `true` and `field` does not exist, the processor quietly exits without modifying the document.",
          "default": false
        },
        "regex_file": {
          "type": "string",
          "description": "The name of the file in the `config/ingest-user-agent` directory containing the regular expressions for parsing the user agent string. Both the directory and the file have to be created before starting Elasticsearch. If not specified, ingest-user-agent will use the `regexes.yaml` from uap-core it ships with."
        },
        "target_field": {
          "type": "string",
          "description": "The field that will be filled with the user agent details.",
          "default": "user_agent"
        },
        "properties": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Controls what properties are added to `target_field`.",
          "default": [
            "name",
            "os",
            "device",
            "original",
            "version"
          ]
        },
        "extract_device_type": {
          "type": "boolean",
          "description": "Extracts device type from the user agent string on a best-effort basis.",
          "default": false
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "field"
      ],
      "description": "The `user_agent` processor extracts details from the user agent string a browser sends with its web requests."
    }
  },
  "description": "Elasticsearch Ingest Node Pipeline Schema"
}
